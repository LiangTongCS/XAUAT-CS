package JavaQimuoBao;
import java.util.regex.Pattern;
import java.util.regex.Matcher;

import java.util.Scanner;

public class ceui111 {


    public static void main(String[] args) {
       /* for (int i = 1;i<5;i++) {
            String regex = "[a-zA-Z]+\\d+"; // 匹配字母后跟数字
            Scanner putin1 = new Scanner(System.in);
            String str = putin1.nextLine();
            boolean flag = str.matches(regex);

            if (flag) {
                System.out.println("String matches the regex.");
            } else {
                System.out.println("String does not match the regex.");
            }
        }


        final String REGEX_TW_CARD = "^\\d{8}|^[a-zA-Z0-9]{10}|^\\83d{15}[0-9xX]$";
        Pattern pattern = Pattern.compile(REGEX_TW_CARD);
        Matcher matcher2 = pattern.matcher("83010119goghc1x");
        System.out.println(matcher2.find());
        for (int i = 1;i<5;i++) {
            String regex = "[a-zA-Z]+\\d+"; // 匹配字母后跟数字
            Scanner putin1 = new Scanner(System.in);
            String str = putin1.nextLine();
            boolean flag = str.matches(regex);

            if (flag) {
                System.out.println("String matches the regex.");
            } else {
                System.out.println("String does not match the regex.");
            }
        }
    }
    */
        Integer obj = 123;
        String str1 = obj.toString();
        System.out.printf("%s",str1);

    }
}
